import React from 'react';

var Playlist = React.createClass({
  render: function() {
    return (
      <div className="playlist">
        Playlist!
      </div>
    );
  }
});

export default Playlist;
